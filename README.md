# goit-algo-hw-04

Домашнє завдання до теми **“Алгоритми сортування”**.

Мета: **емпірично** (через `timeit`) порівняти швидкість трьох підходів до сортування:

- **Insertion Sort** (сортування вставками)
- **Merge Sort** (сортування злиттям)
- **Timsort** (вбудоване сортування Python: `sorted()` / `.sort()`)

---

## 📌 Структура репозиторію

```text
goit-algo-hw-04/
├── task_1.py   # Порівняння алгоритмів сортування (timeit)
├── task_2.py   # (optional) merge_k_lists
├── README.md   # Аналіз та висновки
└── .gitignore
```

---

## ✅ Task 1 — Порівняльний аналіз алгоритмів сортування

### Що тестується

Алгоритми тестуються на різних наборах даних:

- `random`
- `sorted`
- `reversed`
- `nearly_sorted`

Для вимірювання часу використовується модуль **`timeit`**.
Для кожного випадку береться **найкращий час із кількох повторів** (мінімум з `repeat`).

### ▶️ Запуск

**Windows (рекомендовано):**

```bash
py .\task_1.py
```

Опції запуску:

```bash
py .\task_1.py --sizes 1000,2000,5000 --repeat 5
py .\task_1.py --kinds random,sorted,reversed,nearly_sorted
```

> Примітка: Insertion Sort має складність **O(n²)**, тому для великих масивів може працювати дуже довго.

---

## 📊 Результати тестування (timeit)

Нижче наведені результати запуску `task_1.py` (мінімальний час із кількох повторів) для розмірів `1000 / 2000 / 5000`.

### Dataset: random

| Size | Insertion Sort | Merge Sort | Timsort (built-in) |
| ---- | -------------- | ---------- | ------------------ |
| 1000 | 0.015251       | 0.001244   | 0.000056           |
| 2000 | 0.066333       | 0.002659   | 0.000132           |
| 5000 | 0.402142       | 0.009363   | 0.000422           |

### Dataset: sorted

| Size | Insertion Sort | Merge Sort | Timsort (built-in) |
| ---- | -------------- | ---------- | ------------------ |
| 1000 | 0.000073       | 0.001085   | 0.000004           |
| 2000 | 0.000153       | 0.002159   | 0.000009           |
| 5000 | 0.000396       | 0.006640   | 0.000022           |

### Dataset: reversed

| Size | Insertion Sort | Merge Sort | Timsort (built-in) |
| ---- | -------------- | ---------- | ------------------ |
| 1000 | 0.032609       | 0.001058   | 0.000009           |
| 2000 | 0.133045       | 0.003425   | 0.000013           |
| 5000 | 0.801666       | 0.005928   | 0.000023           |

### Dataset: nearly_sorted

| Size | Insertion Sort | Merge Sort | Timsort (built-in) |
| ---- | -------------- | ---------- | ------------------ |
| 1000 | 0.000464       | 0.001014   | 0.000009           |
| 2000 | 0.001541       | 0.002257   | 0.000019           |
| 5000 | 0.010652       | 0.006464   | 0.000048           |

> Часи можуть трохи відрізнятись на різних ПК/системах, але загальна тенденція зберігається.

---

## ✅ Висновки

1. **Timsort (вбудоване сортування Python)** є найшвидшим у всіх тестах. Особливо помітно на `sorted` та `nearly_sorted`, де він використовує структуру даних та оптимізації (комбінація ідей сортування злиттям і вставками).

2. **Insertion Sort**:

   - дуже швидкий на вже відсортованих даних (`sorted`), бо має близьку до лінійної поведінку,
   - але на `random` і особливо `reversed` різко деградує, що відповідає теоретичній складності **O(n²)**.

3. **Merge Sort** демонструє стабільну продуктивність **O(n log n)** і добре масштабується, але в реальних вимірах часто програє **Timsort**, оскільки вбудований алгоритм має додаткові оптимізації та ефективну роботу з частково впорядкованими даними.

**Підсумок:** емпіричні дані підтверджують, що **Timsort** практично найефективніший, тому в більшості задач варто використовувати `sorted()` / `.sort()` замість власних реалізацій.

---

## ⭐ Optional — merge_k_lists

Функція `merge_k_lists` об'єднує `k` відсортованих списків у один відсортований список за принципом злиття (divide & conquer).

### ▶️ Запуск (Windows)

```bash
py .\task_2.py
```

---

## 🧰 Вимоги

- **Python 3.x**
- VS Code або інше IDE
- PowerShell / Terminal
